name: Continuous Integration Workflow
on: [push]

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: install
        run: |
          npm ci
      - name: lint
        run: |
          npm run lint
      - name: build
        run: |
          npm run build
